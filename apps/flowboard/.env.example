# Min. 32 characters. Required for real sessions; use a long random value in production.
# Used by the BFF (iron-session) to seal the httpOnly cookie. Must match in dev and when running E2E against `npm run dev`.
SESSION_SECRET=replace_with_a_long_random_secret_at_least_32_chars
